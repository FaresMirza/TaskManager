# Use a GraalVM JDK image to build the project
FROM ghcr.io/graalvm/graalvm-ce-java17:21.0.5 AS build

# Set the working directory inside the container
WORKDIR /app

# Copy the project's Maven files
COPY . .

# Build the project
RUN mvn clean package -DskipTests

# Use a GraalVM JDK runtime image to run the application
FROM ghcr.io/graalvm/graalvm-ce-java17:21.0.5

# Set the working directory inside the container
WORKDIR /app

# Copy the built JAR file from the build image
COPY --from=build /app/target/*.jar app.jar

# Expose the application's port (adjust according to your app configuration)
EXPOSE 8080

# Run the application
ENTRYPOINT ["java", "-jar", "app.jar"]